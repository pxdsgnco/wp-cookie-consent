# Project Rules

## Git Workflow

- Whenever you are creating worktrees or branches, always be sure to use names that are relatable to the feature being created e.g. `feature/review-project-brief`.
- When implementing a new feature that does not match what the current worktree is meant for, always ask if you should create and use a new worktree for the feature implementation.
